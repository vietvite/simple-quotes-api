{
  "name": "quotes-api-express",
  "version": "0.0.0",
  "author": {
    "name": "Van Viet",
    "email": "vanviet8367@gmail.com"
  },
  "private": true,
  "scripts": {
    "client:install": "yarn --cwd './client' install",
    "client:build": "yarn --cwd './client' build",
    "server:install": "yarn install",
    "start": "yarn client:install && yarn client:build && yarn server:install && node ./bin/www",
    "dev": "nodemon ./bin/www"
  },
  "dependencies": {
    "cookie-parser": "~1.4.3",
    "debug": "~2.6.9",
    "dotenv": "^7.0.0",
    "ejs": "~2.5.7",
    "express": "~4.16.0",
    "http-errors": "~1.6.2",
    "mongoose": "^5.6.0",
    "mongoose-simple-random": "^0.4.1",
    "morgan": "~1.9.0"
  },
  "devDependencies": {
    "nodemon": "^1.19.1"
  }
}
